[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "pyobfuscate"
version = "0.1.0"
description = "Python AST obfuscator"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
where = ["."]
exclude = ["frontend*", "plans*", "IO*", ".venv*"]
